# SDD ledger — plan: docs/superpowers/plans/2026-08-24-assist-model-lifecycle.md

Workspace: `/Users/abhiksarkar/Documents/Projects/personal/labelImg-plus-plus-worktrees/ux-audit-remediation`
Branch: `codex/ux-audit-remediation`
Starting HEAD: `f644c34a6d1a1049e07a727cb534135313902573`
Baseline: Continuous Image Workflow and Video Workspace are complete and independently reviewed. Existing SAM/model suites pass `36 passed / 3 skipped`. Preserve every pre-existing unstaged/untracked user change. Each task stages and commits only its exact owned hunks. Nothing is pushed.

## Pre-flight consistency scan

| Pair / task | Producer and consumer | Finding |
|---|---|---|
| Task 1 | State/manifest tests precede the Qt-free lifecycle and immutable provider metadata they assert. | Internally consistent. Invalid transitions must raise; stale generations are non-mutating `False`; no annotation model is touched. |
| Task 2 | Cache/backend tests precede explicit acquisition, error taxonomy, cancellation cleanup, validation, and cached-only backend loading. | Internally consistent. Existing first-use auto-download must be removed without making the SAM extra mandatory. |
| Task 3 | Panel state tests precede one contextual UI surface and explicit worker orchestration. | Internally consistent with the cancellation ruling below: READY_TO_DOWNLOAD may be projected only after worker cleanup confirms no `.part` remains. |
| Task 4 | Prompt/backend/controller/canvas tests precede box/positive/negative prompt semantics and provisional preview. | Internally consistent. Results stay plain/immutable across the worker boundary and never commit directly. |
| Task 5 | Acceptance/rejection tests precede class resolution, undo/revision/save mutation, focus restoration, and optional Track forward. | Internally consistent. Picker Escape retains the preview; Track forward is explicit and never triggered by acceptance. |
| Task 6 | Offline deterministic lifecycle and live provider checklist consume Tasks 1–5. | Internally consistent. The fake provider may bypass artifact validation because Task 2 separately exercises the real validation boundary; Task 6 validates orchestration and UI state. |
| Tasks 1 → 2 | Task 1's `ModelManifest`/artifacts are consumed by cache lookup and download validation. | Compatible. Size and SHA values remain the single validation source; existing URL constants supply release locations. |
| Tasks 1 → 3 | `AssistSnapshot`, phases, failures, progress, and manifest metadata are projected by the panel. | Compatible. The panel must not invent state outside the Qt-free owner. |
| Tasks 1 → 4 | `AssistPrompt` and document generation are consumed by controller/backend/canvas preview flow. | Compatible. Stale callbacks return without changing preview phase. |
| Tasks 1 → 5 | Provisional `AssistPreview` state is consumed by explicit accept/reject. | Compatible. State review occurs before normal model mutation. |
| Tasks 1 → 6 | Lifecycle and manifest state drive deterministic and live acceptance. | Compatible and directly traceable. |
| Tasks 2 ↔ 3 | Task 2 owns cache/download semantics; Task 3 owns explicit UI submission/cancellation/progress. | Compatible with one ruling: Cancel must wait for the worker's cleanup/finish boundary before projecting ready-to-download; cancellation never retries. |
| Tasks 2 ↔ 4 | Task 2 makes backend loading cached-only; Task 4 changes `predict` from points/labels to `AssistPrompt`. | Compatible if Task 4 preserves Task 2's typed setup/runtime failure boundary and lazy imports. |
| Tasks 2 → 6 | Real provider/cache behavior is exercised once live; offline automation uses controlled substitution. | Compatible. Live acquisition must use explicit consent and validate real final files. |
| Tasks 3 ↔ 4 | Panel tool choice and MainWindow orchestration feed Canvas prompts and controller previews. | Compatible. Closing Assist cancels only provisional prompt/inference ownership. |
| Tasks 3 ↔ 5 | Both touch `assistPanel.py` and `labelImgPlusPlus.py`; Task 3 owns state surfaces and Task 5 owns review actions. | Compatible. Task 5 extends the existing preview state without duplicating buttons or lifecycle ownership. |
| Tasks 3 → 6 | Panel states and accessible controls are captured at 800×600 and 1366×768. | Compatible and directly traceable. |
| Tasks 4 ↔ 5 | Task 4 produces an uncommitted canvas preview; Task 5 transfers it through class/undo/save or clears it without mutation. | Compatible. Exactly one mutation boundary occurs only on acceptance. |
| Tasks 4 → 6 | Prompt refinement and stale-result handling are exercised in lifecycle acceptance. | Compatible. Acceptance helpers must not bypass generation checks. |
| Tasks 5 → 6 | Explicit acceptance and Track-forward eligibility are the final lifecycle outcomes. | Compatible. No propagation handle may exist before the user chooses Track forward. |

## Rulings

- Ruling: Assist download cancellation returns to `READY_TO_DOWNLOAD` only after the cancelled worker has finished cleanup and the temporary artifact is absent — the approved design requires truthful cancellation and removable partial files — cost if wrong: a brief disabled/cancelling state may last until the current network read yields.
- Ruling: Task 6's controlled fake provider validates UI orchestration, not manifest integrity; Task 2 remains the authoritative real-cache validation suite — this avoids tests that merely validate their own fake bytes — cost if wrong: an integration-only cache wiring error must be caught by the live provider pass and Task 2 boundary tests.

## Task progress

- Task 1: implementation complete (`f644c34..7817050`; four new Qt-free state/manifest/test files; focused 11 passed; prior SAM/model baseline 33 passed / 3 skipped; exact staged export 11 passed; isolated slow video nodes passed; Python 3.8/Ruff/diff checks passed).
- Task 1: evidence limitation: the full dirty-worktree suite was bounded after ~80 minutes of pathological Qt style propagation in unrelated video-opening UI tests; it had one unlocalized observed failure. Task 6 retains the complete-suite release gate.
- Task 1: review found 1 Important issue and 1 covering-test Minor: stale generation callbacks can raise after state has advanced out of RUNNING because phase validation occurs before generation rejection.
- Task 1: fix round 1/5 complete (`7817050..3c60aa7`; generation mismatch is rejected before phase validation; stale callbacks after PREVIEW/READY return non-mutating `False`; same-generation invalid phases still raise; Task 1 13 passed; prior SAM/model baseline 33 passed / 3 skipped; exact staged export 13 passed).
- Task 1: complete (`f644c34..3c60aa7`; independent review clean after 1 fix round; 13 focused tests passed; existing SAM/model baseline 33 passed / 3 skipped; exact staged export and compatibility/static checks passed).
- Task 2: implementation complete (`3c60aa7..a4775c7`; explicit one-shot download, cooperative cancellation/cleanup, size/SHA validation and atomic promotion, distinct error taxonomy, validated cache lookup, cached-only backend loading; 33 passed / 3 optional skips; import/static/diff checks passed).
- Task 2: review found 2 Important issues (final size/SHA validation occurs after fsync; backend tests mock internal cache/resolver boundaries) and 1 downstream docstring Minor assigned to Task 3.
- Task 2: fix round 1/5 implemented (`a4775c7..1ce165b`; final byte count/SHA validation now precedes flush/fsync; backend coverage uses the real resolver/cache path with only external URL/runtime substitution; focused Task 2/Task 1/SAM suite 35 passed / 3 optional skips; import/static/diff checks passed).
- Task 2: complete (`3c60aa7..1ce165b`; independent scoped re-review passed after 1 fix round with no new Critical/Important findings; accepted stale `sam_controller.py` docstring Minor remains assigned to Task 3).
- Task 2: reopened by Task 6 live provider chronology at `ed5f304`: `urlopen()` has no timeout, so a stalled socket connect/read cannot observe cancellation and leaves UI `Cancelling…` indefinitely.
- Task 2: fix round 2/5 initial commit (`ed5f304..387cbfe`; 2.0-second urllib connect/read bound, cancelled socket timeout/URL errors become ModelDownloadCancelled, uncancelled timeout remains typed offline, one request/no retry/cleanup semantics preserved; focused compatibility 101 passed / 3 optional skips; native Sky Cancel returned Ready in 1,147 ms and stayed empty/no-part/no-retry for 30 seconds; Python 3.8/Ruff/diff clean).
- Task 2: scoped review found 1 Important cross-version gap: built-in `TimeoutError` could escape the socket-timeout handler. Fix-round follow-up `387cbfe..96bf9f3` handles and tests both timeout classes; focused compatibility 102 passed / 3 optional skips.
- Task 2: complete after native reopening (`3c60aa7..96bf9f3`; second scoped re-review passed with no Critical/Important findings; real cancellation bound and 30-second no-retry observation accepted for the fixed boundary).
- Task 2: reopened again by Task6 explicit retry at `96bf9f3`: the 2-second socket bound plus 1 MiB body reads times out a progressing real CDN, and retry redownloads an already manifest-valid encoder instead of continuing with the missing decoder.
- Task 2: fix round 3/5 initial commit (`96bf9f3..46bd35a`; separate 64 KiB network reads retain the 2-second cancellation bound; manifest-valid final artifacts are reused in order, stale `.part` removed, tampered finals redownloaded, progress accounts for reused completion; cache 19 passed and focused compatibility 105 passed / 3 optional skips; Python 3.8/Ruff/diff clean).
- Task 2: scoped review found 1 Important progress-bound issue: an overlong chunked body can emit raw progress above manifest total before final validation, especially after reused bytes. Follow-up `46bd35a..509650a` validates artifact overrun before aggregate accounting/callback; cache 20 passed and focused compatibility 106 passed / 3 optional skips; re-review passed with no new Critical/Important findings.
- Task 2: complete after release/live fixes (`3c60aa7..509650a`; bounded cross-version cancellation, 64 KiB slow-stream reads, manifest-valid artifact reuse, and bounded truthful progress independently approved).
- Task 2: reopened by Task6 decoder-only retry at `509650a`: the real CDN progressed with 64 KiB reads but had a healthy idle gap longer than the production 2.0-second socket timeout.
- Task 2: fix round 4/5 complete (`509650a..fbc74b4`; production urllib operation bound raised to a documented finite 10.0 seconds while explicit short test timeouts remain exact; cache 21 passed and focused compatibility 107 passed / 3 optional skips; Python 3.8/Ruff/diff clean; independent scoped re-review passed after the sole stale test-description Minor was corrected). Live retry may resume.
- Task 2: complete after final Task 6 live revalidation (`3c60aa7..fbc74b4`; independently approved finite cancellation/idle bounds, 64 KiB reads, valid-artifact reuse, bounded progress, and timeout taxonomy; explicit native decoder-only Retry reused the encoder unchanged, reached Ready, matched both manifest hashes/sizes, and left no `.part`).
- Task 3: implementation complete (`1ce165b..fdb9004`; one contextual Assist drawer, explicit Box/Points choice, explicit one-shot acquisition, typed recovery, truthful running-cancel cleanup, custom/cache readiness, and stale controller docstring correction; exact exported gates total 72 passed / 3 optional skips; Python 3.8/Ruff/diff checks passed; unrelated dirty hunks preserved).
- Task 3: independent review found 3 Important issues: optional cache filesystem errors can escape startup; cancelling a queued SAM-lane download can remain stuck because pending cancellation has no finished signal; cached/custom model files can project READY when the optional runtime is absent.
- Task 3: fix round 1/5 implemented (`fdb9004..d113903`; cache failures become typed optional-feature recovery; READY requires valid paths plus runtime; Retry reuses valid local files; targeted pending/running/inactive cancellation disposition reconciles never-started jobs immediately while preserving running cleanup; exact exported focused/compatibility gates passed, Python 3.8/Ruff/diff clean).
- Task 3: complete (`1ce165b..d113903`; independent re-review passed after 1 fix round with no remaining Critical/Important/Minor findings; focused reviewer verification 30 passed; no push).
- Task 3: reopened by Task 6 native computer-use acceptance at `2bf75a7`: Smart Select routes correctly but the Assist panel is an unmanaged `QStackedWidget` child behind the current page, producing no native pixels or AX nodes despite `isVisible()`/focus tests passing.
- Task 3: fix round 2/5 implemented (`2bf75a7..3e44b55`; zero-space page surface owns stack/video/Assist as sibling layers; close invalidates exposed stack region to prevent Cocoa ghost paint; exact exported Task 3–5 gates passed; real macOS Sky paint/AX open+close verification passed; Python 3.8/Ruff/diff clean).
- Task 3: complete after native reopening (`1ce165b..3e44b55`; independent re-review passed fix round 2 with no Critical/Important/Minor findings; focused reviewer gate 9 passed; no push).
- Task 4: implementation complete (`d113903..1562ebd`; immutable Smart Box/Points prompts, positive/negative/box backend semantics, generation-checked worker previews, paint-only canvas/MainWindow projection, transient cleanup, and strict no-commit/no-save boundary; exact exported 86 passed / 3 optional skips; Python 3.8/Ruff/diff clean; unrelated dirty hunks preserved).
- Task 4: independent review found 1 Important issue: video frame publication and same-path session reopen do not invalidate in-flight Assist results or file-path-keyed embeddings, allowing a frame A result/cache to publish or be reused on frame B.
- Task 4: fix round 1/5 implemented (`1562ebd..3dd1c9a`; explicit video session+frame content identity invalidates requests/embeddings at first-frame and frame publication; stale frame completions cannot publish; same-path reopen and frame changes re-embed current QImage; provisional-only invalidation; exact staged export 124 passed / 3 optional skips; Python 3.8/Ruff/diff clean).
- Task 4: complete (`d113903..3dd1c9a`; independent re-review passed after 1 fix round with no remaining Critical/Important/Minor findings; focused reviewer gates passed; no push).
- Task 5: implementation complete (`3dd1c9a..be147f5`; Enter/Escape preview review, active/inline-picker class resolution, exactly one normal image/video undo+revision+continuous-save boundary, mutation-free rejection, manual video anchor, and explicit-only Track Forward; exact exported Task 5 30 passed plus compatibility 94 passed / 2 optional skips; Python 3.8/Ruff/diff clean; unrelated dirty hunks preserved).
- Task 5: independent review found 1 Important issue (Assist picker ownership survives tool exit and can trap the next manual provisional shape) and 1 Minor issue (Track Forward stays visibly enabled after anchor undo/delete until clicked).
- Task 5: fix round 1/5 implemented (`be147f5..2bf75a7`; tool transitions dismiss only Assist picker ownership so the next manual picker commits normally; video-model mutations immediately invalidate/reproject stale Track Forward offers; exact exported Task 5 33 passed plus compatibility 95 passed / 2 optional skips; Python 3.8/Ruff/diff clean).
- Task 5: complete (`3dd1c9a..2bf75a7`; independent re-review passed after 1 fix round with no remaining Critical/Important/Minor findings; narrow reviewer gate 5 passed; no push).
- Task 6: implementation and acceptance complete at `bb09aa3` from reviewed head `3e44b55`; deterministic cancellation/retry/review/save/explicit-track lifecycle and native QAction regression pass; socket-sentinel Assist/model aggregate passes `97 passed / 3 optional skips` in the dirty worktree and exact index export.
- Task 6: live real-provider pass complete in isolated cache/project/runtime space; explicit Cancel left no `.part` and no 30-second auto retry; explicit retry produced manifest-valid MobileSAM artifacts; real image and supplied-video preview reject/accept/save passed; Track Forward started only after the explicit action and produced 150 pending / 0 gaps / 0 failures on the task-local video copy.
- Task 6: evidence complete with 20 controlled production-Qt PNGs at exact 800x600 and 1366x768 plus four truthful 1295x768 native video JPEGs; Python 3.8 AST/compile, Ruff F, screenshot integrity/dimensions, and staged diff checks pass.
- Task 6: bounded exact full run repeated the known cumulative native Qt pathology and was stopped after 3m48s at ~36% with no failures; fresh-process segmentation exercised all 1322 nodes (`1314 passed / 5 reproducible pre-existing exact-HEAD failures / 3 skipped`). Plugin packaging passes without the offline sentinel. Exact failure names and causes are recorded in `task-6-report.md`; Task 6 focused/aggregate gates are green. No push.
- Task 6: independent review FAIL at `bb09aa3`: release blockers are simultaneous manual-draft/Assist ownership, over-mocked controller orchestration in the E2E test, semantically false exact-size post-accept captures, clipped essential Assist actions, missing committed canvas accessible name, and insufficient retained cancel/no-retry chronology; three stale compatibility tests/fixtures and one evidence count also require correction. Prerequisite fix sequence in progress before re-review.
- Task 3: fix round 3/5 implemented (`bb09aa3..c7a3aa5`; phase-aware two-column action grid keeps Smart Box/Points and full-width Track Forward readable at 800x600 and 1366x768; exact `Annotation canvas` accessible-name line committed; focused exact-export Task 3–5 gates and Python 3.8/Ruff/diff checks passed; unrelated dirty Canvas hunks preserved).
- Task 3: complete after release review (`1ce165b..c7a3aa5`; independent fix-round-3 re-review passed with no Critical/Important/Minor findings; focused reviewer gate 6 passed). The downstream class-filter name remains routed to final release-gate cleanup.
- Task 4: reopened for fix round 2/5 from `c7a3aa5` to resolve simultaneous manual-draft/class-picker and Assist prompt ownership, and replace the stale low-level SAM blocked-click expectation with supported user-flow coverage.
- Task 4: fix round 2/5 implemented (`c7a3aa5..ed5f304`; one activation-boundary `_discard_workflow_draft()` call clears/balances pending manual shape, picker, workflow and gesture ownership before Smart tools; real rectangle/polygon→Box/Points flows and rewritten supported Canvas order pass; exact export 202 passed / 3 optional skips with separately routed accessibility node excluded; Python 3.8/Ruff/diff clean).
- Task 4: complete after release review (`d113903..ed5f304`; independent fix-round-2 re-review passed with no Critical/Important/Minor findings; required 5, compatibility 24, and complete owned 89 tests passed).
- Task 6: fix round 1/5 in progress from `ed5f304`: real controller-worker orchestration in deterministic lifecycle, truthful video post-accept captures, retained cancel/no-retry chronology, stale compatibility test/fixture updates, downstream workspace accessibility-name cleanup, evidence count reconciliation, and zero-failure segmented exact gate.
- Task 6: fix round 1 resumed from reviewed head `96bf9f3`; focused real-controller/stale-test/accessibility corrections remain uncommitted/preserved, and truthful video captures, retained timestamped provider chronology, docs, exact staging, and zero-failure segmented gate remain.
- Task 6: fix round 1 resumed from reviewed head `509650a`; retained cancel/no-retry transcript and valid encoder remain in isolated acceptance state, and explicit missing-decoder retry/final hashes, truthful captures, docs, staging, and zero-failure segmented gate remain.
- Task 6: paused after reviewed reuse was proven (encoder unchanged, decoder-only request, cleanup/no part) because the real provider exposed the Task2 idle-gap timeout above. Persistent transcript and uncommitted corrections remain preserved.
- Task 6: fix round 1/5 acceptance complete on reviewed head `fbc74b4`: deterministic lifecycle now retains real SamController/coordinator/signals with only an external fake inference backend; stale image/video/decoder fixtures follow the current preview/picker/accept contract; exact downstream accessibility names staged without unrelated dirty hunks; all 20 controlled PNGs regenerated with a real task-local video/accepted anchor for post-accept; timestamped native Cancel/no-part/no-auto-retry/explicit decoder-only Retry transcript retained; final real encoder/decoder sizes and SHA values match the manifest with no `.part`.
- Task 6: final release gates green: focused release findings 7 passed; socket-sentinel Assist aggregate 109 passed / 2 optional skips; exact fresh-process segmentation covered all 1,338 nodes with corrected total 1,335 passed / 3 optional skips / 0 failed; Python 3.8 AST/compile, Ruff F, 24-artifact integrity/dimensions, and staged diff checks pass. Final correction exact-stage is ready; no push.
